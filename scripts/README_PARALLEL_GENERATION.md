# 并发生成预生成数据集指南

## 概述

为了解决大型词汇书生成速度慢和文件大小超过GitHub限制的问题，我们引入了分片和并发生成机制。

**关键改进：**
- ⚡ **速度提升**：通过并发处理，8个并发进程可实现约8-10倍加速
- 📦 **文件大小控制**：将大文件分割成多个小文件，每个文件约4-5MB，远低于GitHub的25MB限制
- 🔄 **断点续传**：每个分片独立管理进度，失败的分片可单独重新生成
- 🌐 **前端兼容**：自动适配多文件加载，向后兼容单文件模式
- 📁 **独立目录**：每本单词书的分片文件存放在独立子目录中，便于管理

**目录组织结构：**
```
wordlists/
  cet4_edited/             # CET4词汇书的分片目录
    part1.txt
    part2.txt
    ...
  cet6_edited/             # CET6词汇书的分片目录
    part1.txt
    part2.txt
    ...
  toefl/                   # TOEFL词汇书的分片目录
    part1.txt
    part2.txt
    ...
data/
  cet4_edited/             # CET4生成数据目录
    cet4_edited_index.json
    cet4_edited_pregenerated_part1.json
    cet4_edited_pregenerated_part2.json
    ...
  cet6_edited/             # CET6生成数据目录
    cet6_edited_index.json
    cet6_edited_pregenerated_part1.json
    ...
```

## 工作流程

### 1. 分片单词书（split-wordlist.js）

将大型单词书分割成多个小文件，生成索引文件。

**使用方法：**

```bash
# 基础用法：分成10个分片（默认）
node split-wordlist.js --input ../languagelearning/english/wordlists/CET4_edited.txt

# 指定分片数量
node split-wordlist.js --input ../languagelearning/english/wordlists/CET4_edited.txt --parts 10

# 按每片单词数分割
node split-wordlist.js --input ../languagelearning/english/wordlists/CET4_edited.txt --words-per-part 500

# 指定词汇书ID
node split-wordlist.js --input ../languagelearning/english/wordlists/CET4_edited.txt --book-id cet4_edited
```

**参数说明：**
- `--input <path>`：输入单词书文件路径（必需）
- `--output-dir <path>`：输出目录（默认：输入文件所在目录）
- `--parts <number>`：分片数量（默认：10）
- `--words-per-part <number>`：每个分片的单词数（优先于parts）
- `--book-id <id>`：词汇书ID（默认：从文件名推断）

**输出文件：**
- `wordlists/cet4_edited/part1.txt, part2.txt, ...`：分片后的单词列表（每本单词书在独立子目录中）
- `data/cet4_edited/cet4_edited_index.json`：索引文件，包含所有分片的元数据

### 2. 并发生成（generate-parallel.js）

并发运行多个生成进程，大幅提升生成速度。

**使用方法：**

```bash
# 基础用法：使用8个并发进程
node generate-parallel.js --book-id cet4_edited --language english

# 指定并发数量
node generate-parallel.js --book-id cet4_edited --language english --concurrency 10

# 自定义RPM限制
node generate-parallel.js --book-id cet4_edited --language english --rpm 60

# 测试模式：只处理前2个分片
node generate-parallel.js --book-id cet4_edited --language english --test-mode 2

# 强制重新生成（清除现有进度）
node generate-parallel.js --book-id cet4_edited --language english --force
```

**参数说明：**
- `--book-id <id>`：词汇书ID（必需）
- `--language <lang>`：语言（english/japanese，默认：english）
- `--concurrency <number>`：并发进程数（默认：8）
- `--rpm <number>`：总RPM限制（默认：60）
- `--test-mode <number>`：测试模式，只处理前N个分片
- `--force`：强制从头开始，删除现有进度

**RPM分配机制：**
- 总RPM限制会平均分配给所有并发进程
- 例如：60 RPM / 8 进程 = 每进程 7.5 RPM
- 每个进程独立控制速率，避免API限流

**监控输出：**
- 实时显示每个分片的进度
- 格式：`[Part N] 处理单词: xxx`
- 完成时显示总耗时和成功/失败统计

### 3. 前端自动加载

前端加载器已自动适配多文件模式：

**工作原理：**
1. 首先尝试加载 `data/${bookId}/${bookId}_index.json` 索引文件（新结构）
2. 如果索引不存在，尝试加载 `data/${bookId}_index.json`（旧结构，向后兼容）
3. 如果索引存在，并发加载所有分片文件
4. 如果索引不存在，回退到单文件模式（向后兼容）
5. 将所有分片数据合并为统一的数据对象

**无需修改应用代码**，加载器自动处理多文件逻辑。

## 完整工作流示例

### 示例：生成CET4词汇书

```bash
# 步骤1：分片单词书（4544个单词 → 10个分片，每个约450个单词）
cd scripts
node split-wordlist.js --input ../languagelearning/english/wordlists/CET4_edited.txt --parts 10

# 输出：
# 分片完成！
# 总单词数: 4544
# 分片数量: 10
# 平均每片: 455 个单词
#
# 生成的文件：
# wordlists/cet4_edited/part1.txt
# wordlists/cet4_edited/part2.txt
# ...
# data/cet4_edited/cet4_edited_index.json

# 步骤2：并发生成（8个并发进程）
node generate-parallel.js --book-id cet4_edited --language english --concurrency 8

# 预期耗时：
# 单线程：约90小时（1.2分钟/词 × 4544词）
# 8并发：约11-12小时（90 ÷ 8）

# 步骤3：验证生成结果
ls -lh ../languagelearning/english/data/cet4_edited/cet4_edited_pregenerated_part*.json

# 输出：
# -rw-r--r-- 1 user user 4.2M cet4_edited_pregenerated_part1.json
# -rw-r--r-- 1 user user 4.5M cet4_edited_pregenerated_part2.json
# ...
```

## 文件大小估算

**估算公式：**
- 每个单词的AI生成内容约9-10KB
- 500个单词 ≈ 4.5-5MB
- 推荐分片策略：

| 总单词数 | 推荐分片数 | 每片单词数 | 单文件大小 |
|---------|----------|----------|----------|
| 1000    | 2        | 500      | ~5MB     |
| 2000    | 4        | 500      | ~5MB     |
| 4544    | 10       | 454      | ~4.5MB   |
| 10000   | 20       | 500      | ~5MB     |

## 断点续传和错误恢复

### 中断恢复

如果生成过程中中断（Ctrl+C、网络错误等）：

```bash
# 直接重新运行相同命令，自动从上次中断处继续
node generate-parallel.js --book-id cet4_edited --language english
```

每个分片的进度保存在：
- `data/cet4_edited/cet4_edited_pregenerated_part1.json.progress.json`
- `data/cet4_edited/cet4_edited_pregenerated_part2.json.progress.json`
- ...

### 重新生成失败的分片

如果某个分片失败，可以单独重新生成：

```bash
# 方法1：使用generate-pregenerated.js直接生成单个分片
node generate-pregenerated.js \
  --input ../languagelearning/english/wordlists/cet4_edited/part5.txt \
  --output ../languagelearning/english/data/cet4_edited/cet4_edited_pregenerated_part5.json \
  --bookId cet4_edited \
  --language english

# 方法2：使用--force强制全部重新生成
node generate-parallel.js --book-id cet4_edited --language english --force
```

## 性能优化建议

### 并发数选择

| API提供商 | RPM限制 | 推荐并发数 | 每进程RPM |
|---------|--------|----------|----------|
| OpenAI   | 60     | 6-8      | 7.5-10   |
| DeepSeek | 60     | 6-8      | 7.5-10   |
| 本地模型  | 无限制  | 4-16     | 根据硬件 |

**建议：**
- 保守策略：6个并发（10 RPM/进程）
- 平衡策略：8个并发（7.5 RPM/进程）
- 激进策略：10个并发（6 RPM/进程）

### 分片策略

**推荐分片大小：**
- 每片400-600个单词
- 目标文件大小：4-6MB
- 确保文件小于GitHub的25MB限制

**分片数量权衡：**
- 分片太少：文件过大，无法上传GitHub
- 分片太多：管理复杂，加载性能下降
- 最佳实践：10-20个分片

## 故障排查

### 问题1：索引文件不存在

**错误信息：**
```
[错误] 无法读取索引文件: .../data/cet4_edited/cet4_edited_index.json
```

**解决方案：**
```bash
# 先运行分片工具生成索引
node split-wordlist.js --input ../languagelearning/english/wordlists/CET4_edited.txt
```

### 问题2：分片文件缺失

**错误信息：**
```
[Part 5] ✗ 失败 (退出代码: 1)
```

**解决方案：**
```bash
# 检查wordlists目录是否存在所有分片
ls ../languagelearning/english/wordlists/cet4_edited/

# 如果缺失，重新运行分片工具
node split-wordlist.js --input ../languagelearning/english/wordlists/CET4_edited.txt
```

### 问题3：API速率限制

**错误信息：**
```
[Part 3] [LLMClient] 请求失败: 429 Too Many Requests
```

**解决方案：**
```bash
# 方案1：减少并发数
node generate-parallel.js --book-id cet4_edited --concurrency 4

# 方案2：降低总RPM
node generate-parallel.js --book-id cet4_edited --rpm 30

# 方案3：等待一段时间后重试（自动断点续传）
```

### 问题4：前端加载失败

**浏览器控制台错误：**
```
[PregeneratedDataLoader] 分片2加载失败: 404
```

**检查清单：**
1. 确认所有分片文件已生成并上传
2. 检查索引文件中的路径是否正确
3. 验证文件名格式：`cet4_edited_pregenerated_part1.json`
4. 确保文件位于正确的`data/cet4_edited/`目录

## 技术细节

### 索引文件格式

```json
{
  "version": "1.0",
  "bookId": "cet4_edited",
  "language": "english",
  "totalWords": 4544,
  "totalParts": 10,
  "generated": "2025-11-05T10:00:00.000Z",
  "parts": [
    {
      "partId": 1,
      "wordlistFile": "wordlists/cet4_edited/part1.txt",
      "outputFile": "data/cet4_edited/cet4_edited_pregenerated_part1.json",
      "wordCount": 455,
      "wordRange": {
        "start": 1,
        "end": 455,
        "firstWord": "a",
        "lastWord": "apartment"
      }
    },
    ...
  ]
}
```

### 进程间协调

- **无通信设计**：各进程完全独立，无需IPC
- **资源隔离**：每个进程处理不同的单词子集
- **进度独立**：每个进程维护自己的进度文件
- **速率独立**：每个进程独立控制API请求速率

### 数据合并策略

```javascript
// 前端加载器合并逻辑
const parts = await Promise.all(loadPromises);
this.data = Object.assign({}, ...parts);
```

- 使用`Object.assign()`合并所有分片数据
- 并发加载所有分片（使用`Promise.all()`）
- 加载失败的分片返回空对象，不影响其他分片

## 最佳实践总结

1. **分片策略**：
   - 每片400-600个单词
   - 目标文件大小4-6MB
   - 总分片数10-20个

2. **并发配置**：
   - 起始并发：8个进程
   - 总RPM：60（API限制）
   - 每进程RPM：7.5

3. **生成流程**：
   - 先运行分片工具
   - 再运行并发生成
   - 验证生成结果

4. **错误处理**：
   - 中断后直接重新运行（自动续传）
   - 失败的分片可单独重新生成
   - 使用`--test-mode`先测试小规模

5. **版本控制**：
   - 分片文件（wordlists/*.txt）提交到Git
   - 索引文件（data/*_index.json）提交到Git
   - 生成文件（data/*_pregenerated_part*.json）根据大小决定是否提交

## 升级指南

如果你之前使用的是单文件生成模式：

### 迁移步骤

```bash
# 1. 分片现有单词书
node split-wordlist.js --input ../languagelearning/english/wordlists/CET4_edited.txt

# 2. 并发生成（会自动识别并使用索引文件）
node generate-parallel.js --book-id cet4_edited --language english

# 3. 前端自动适配（无需修改代码）
# 加载器会自动检测索引文件并使用多文件模式
```

### 向后兼容性

- 前端加载器支持新旧两种目录结构：
  - 新结构：`data/${bookId}/${bookId}_index.json`（优先）
  - 旧结构：`data/${bookId}_index.json`（回退）
- 如果没有索引文件，自动回退到单文件模式
- 现有单文件数据仍然可以正常使用
- 无需修改任何应用代码

## 总结

通过分片和并发机制，我们实现了：

| 指标 | 单文件模式 | 多文件并发模式 | 改进 |
|------|----------|--------------|------|
| 生成速度 | 90小时 | 11小时 | ⚡ 8倍加速 |
| 文件大小 | 40MB | 4-5MB/片 | 📦 符合GitHub限制 |
| 容错性 | 全部重来 | 独立重试 | 🔄 断点续传 |
| 加载性能 | 一次加载 | 并发加载 | 🌐 更快的首次加载 |

**开始使用并发生成功能，大幅提升开发效率！**
